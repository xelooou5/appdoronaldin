python-telegram-bot==20.3
python-dotenv
Pillow
openai
requests
# Only keep minimal dependencies for production.
# Add: Ecreasy and HuggingFace API support (no heavy ML libs)
# Remove: google-genai, groq
