python-telegram-bot
google-generativeai
python-dotenv
Pillow
openai
requests
fastapi
uvicorn
groq
# deepseek and codegeex SDKs might not be standard.
# We will use 'requests' for them if the specific SDKs are causing bloat or install issues.
# For now, let's keep it minimal.
